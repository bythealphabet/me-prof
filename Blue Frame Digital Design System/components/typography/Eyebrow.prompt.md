Monospace "// kicker" that sits above a heading — the signature blueprint label.

```jsx
<Eyebrow>Blue Wave Based</Eyebrow>
<Eyebrow prefix="">No marker</Eyebrow>
```

`prefix` defaults to `//`; pass `''` to drop it. Pairs with `SectionHeading`.
